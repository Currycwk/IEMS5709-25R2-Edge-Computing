apiVersion: v1 # The version of the Kubernetes API to use
kind: ConfigMap # The type of Kubernetes object, used to store non-confidential data
metadata:
  name: hello-k3s-app # The name of the ConfigMap
data:
  app.py: | # The key is 'app.py', and its value is a multiline string containing the Python script
    #!/usr/bin/env python3
    from http.server import HTTPServer, BaseHTTPRequestHandler
    import json, os

    class Handler(BaseHTTPRequestHandler):
        def do_GET(self):
            self.send_response(200)
            self.send_header("Content-Type", "application/json")
            self.end_headers()
            body = json.dumps({
                "message": "Hello from K3s!",
                "pod": os.environ.get("HOSTNAME", "unknown"),
            })
            self.wfile.write(body.encode())

        def log_message(self, fmt, *args):
            print(fmt % args, flush=True)

    if __name__ == "__main__":
        server = HTTPServer(("0.0.0.0", 5000), Handler)
        print("Server running on http://0.0.0.0:5000", flush=True)
        server.serve_forever()
